---
description: Фиксация ошибки агента в реестр (Fault Registry)
---
// turbo-all

1. Агент анализирует совершенную ошибку.
2. Запускаем запись в реестр через скрипт.

```powershell
# Пример вызова (замените аргументы на реальные данные об ошибке)
python "e:\Downloads\--ANTIGRAVITY store\IDE-optimus\PROJECT\scripts\utils\record_error.py" `
    "CODE_STYLE" `
    "Использование inline стилей вместо CSS классов" `
    "Забыт параметр в конфиге генератора" `
    "Перенос стилей в тег <style>" `
    "Инспектировать inline-styles во всех HTML генераторах перед коммитом."
```

1. Обновляем память проекта, если ошибка критическая.
