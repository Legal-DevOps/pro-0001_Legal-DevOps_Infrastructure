---
description: Start a clean session without any specific project context
---

This command initializes a "Zero Context" session. No specific project from the archive is loaded. Use this for general brainstorming, one-off analyses, or ad-hoc tasks that don't need to be tracked in a Case Vault.

### 🧹 Action: Reset Focus

The agent will now treat the conversation as a "Clean Slate". No project-specific data (`parties.json`, `tasks.json`) from the existing cases will be used until a project is explicitly selected.

#### Recommended Next Steps

- Perform general research.
- Create temporary scripts in `/tmp/`.
- Draft documents without saving them to a specific `archive/` folder.
